export class Calculator {
    sum(num1, num2) {
        return num1 + num2;
    }

    substract(num1, num2) {
        return num1 - num2;
    }

    multiple(num1, num2) {
        return num1 * num2;
    }

    divide(num1, num2) {
        return Math.floor(num1 / num2);
    }
}
